# 分布式相控阵使能的物联网高精度定位——项目背景与技术路线

---

## 一、研究背景

### 1. 室内高精度定位的需求

随着物联网飞速发展，室内高精度定位需求日益迫切：仓储物流需要厘米级精度定位货架与机器人，大型商场需要米级导航引导顾客。然而室外成熟的GPS等卫星定位在室内因建筑遮挡而信号急剧衰减甚至完全消失，同时室内环境的信号反射、折射、散射形成严重的"多径效应"，致使传统定位方法基本失灵。

### 2. 现有主流方案及其困境

高精度定位的实现方案主要分为两类：

- **时延类（TOA/TDOA/RTOF）**：通过信号到达时间或时间差进行三边/双曲线定位，可实现亚米级精度。致命缺陷在于对物联网场景成本过高：（1）2D定位至少需要三个接收器；（2）接收器时钟精度要求为纳秒级。物联网设备造价低廉、数量众多，此类方法不适用。

- **角度类（AOA）**：仅需两个接收器获取信号来向，利用方向直线相交即可实现2D定位，精度高、成本低，符合物联网设备需求。

### 3. AOA定位面临的两个核心问题

- **问题1**：如何获取每根天线上的相位信息？相控阵只能测得合信号功率，传统方案依赖矢量网络分析仪等昂贵设备。
- **问题2**：如何将相位信息转化为到达角？多径效应与多信源干扰下，需要区分有效信号与噪声方向。

---

## 二、技术路线总览

技术路线的核心链路为：

> **REV算法**（能量→相位）→ **Beamforming / MUSIC算法**（相位→角度）→ **AOA定位**（角度→位置）

同时辅以 **MATLAB 算法仿真 → FEKO/Wireless Insight 电磁仿真 → 8×8相控阵硬件实测** 的完整验证体系。

---

## 三、三大核心算法的公式与过程

### 算法一：REV（Rotating Electric-field Vector）算法

**目的**：仅通过测量合成信号的功率（纯能量，无需复数信号），反演出每根天线的相对幅度与相对相位，从而将能量信息转换为相位信息。

**原理与公式推导**：

设有 $N$ 元天线阵列，对目标单元 $m$ 依次施加相位扰动 $\Delta = 0^\circ, 90^\circ, 180^\circ$，测量合成功率 $P_0, P_{90}, P_{180}$。

令单元 $m$ 的复场为 $a = E_m \cdot e^{j\phi_m}$，其余所有单元合成复场为 $b = \bar{E}_m \cdot e^{j\bar{\phi}_m}$。施加相位扰动 $\Delta$ 后的合成场为：

$$E_{\text{comb}}(\Delta) = b + a \cdot e^{j\Delta}$$

合成功率：

$$P(\Delta) = |E_{\text{comb}}(\Delta)|^2 = |b|^2 + |a|^2 + a\bar{b} \cdot e^{-j\Delta} + \bar{a}b \cdot e^{j\Delta}$$

令 $r = a\bar{b} = E_m \bar{E}_m \cdot e^{j(\phi_m - \bar{\phi}_m)}$，代入三种扰动情况：

$$
\begin{cases}
P_0 = |b|^2 + |a|^2 + r + \bar{r} \\[4pt]
P_{90} = |b|^2 + |a|^2 - jr + j\bar{r} \\[4pt]
P_{180} = |b|^2 + |a|^2 - r - \bar{r}
\end{cases}
$$

构造线性方程组 $\mathbf{A}\mathbf{X} = \mathbf{P}$：

$$
\begin{bmatrix} 1 & 1 & 1 \\ 1 & -j & j \\ 1 & -1 & -1 \end{bmatrix}
\begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} =
\begin{bmatrix} P_0 \\ P_{90} \\ P_{180} \end{bmatrix}
$$

求解得：

$$x_1 = |a|^2 + |b|^2 = E_m^2 + \bar{E}_m^2, \quad x_2 = r, \quad x_3 = \bar{r}$$

从 $x_2$ 提取相位差：

$$\Delta\phi = \phi_m - \bar{\phi}_m = \arg(x_2)$$

利用代数恒等式恢复幅度：

$$
\begin{aligned}
\bar{E}_m &= \frac{\sqrt{|x_1| + 2|x_2|} + \sqrt{|x_1| - 2|x_2|}}{2} \\[4pt]
E_m &= \frac{\sqrt{|x_1| + 2|x_2|} - \sqrt{|x_1| - 2|x_2|}}{2}
\end{aligned}
$$

最终相对场（复信道频率响应）：

$$k_m \cdot e^{jX_m} = \frac{E_m}{E_m + \bar{E}_m \cdot e^{j\Delta\phi}}$$

---

### 算法二：MUSIC（Multiple Signal Classification）算法

**目的**：基于信号协方差矩阵的特征分解，将接收数据空间分离为信号子空间与噪声子空间，利用二者正交性实现高精度到达角估计。项目采用**二维MUSIC**，可同时估计方位角 $\theta$ 和俯仰角 $\phi$。

**原理与公式推导**：

#### 第一步：阵列接收模型

$N$ 元均匀线阵，接收 $M$ 个窄带远场信号源（$M < N$），来自方向 $\theta_1, \theta_2, \ldots, \theta_M$：

$$\mathbf{x}(t) = \mathbf{A}(\boldsymbol{\theta})\mathbf{s}(t) + \mathbf{n}(t)$$

其中：

- $\mathbf{x}(t) \in \mathbb{C}^{N\times1}$ —— 阵列接收信号向量
- $\mathbf{A}(\boldsymbol{\theta}) = [\mathbf{a}(\theta_1), \mathbf{a}(\theta_2), \ldots, \mathbf{a}(\theta_M)]$ —— $N \times M$ 阵列流型矩阵
- $\mathbf{s}(t) \in \mathbb{C}^{M\times1}$ —— 信号源向量
- $\mathbf{n}(t) \in \mathbb{C}^{N\times1}$ —— 加性高斯白噪声

对于均匀线阵，导向矢量为：

$$\mathbf{a}(\theta) = \begin{bmatrix} 1 \\ e^{-j\frac{2\pi d}{\lambda}\sin\theta} \\ e^{-j\frac{4\pi d}{\lambda}\sin\theta} \\ \vdots \\ e^{-j\frac{2\pi(N-1)d}{\lambda}\sin\theta} \end{bmatrix}$$

> 二维MUSIC中 $\mathbf{a}(\theta, \phi)$ 同时依赖方位角与俯仰角，对于矩形平面阵，导向矢量变为二者Kronecker积形式。

#### 第二步：协方差矩阵

$$\mathbf{R} = \mathbb{E}[\mathbf{x}(t)\mathbf{x}^H(t)] = \mathbf{A}\mathbf{R}_s\mathbf{A}^H + \sigma^2\mathbf{I}$$

其中 $\mathbf{R}_s = \mathbb{E}[\mathbf{s}(t)\mathbf{s}^H(t)]$ 为信号协方差矩阵（满秩 $M$），$\sigma^2$ 为噪声功率。

#### 第三步：特征分解——子空间分离

对 $\mathbf{R}$ 做特征分解：

$$\mathbf{R} = \sum_{i=1}^{N} \lambda_i \mathbf{u}_i \mathbf{u}_i^H = \mathbf{U}_s \mathbf{\Lambda}_s \mathbf{U}_s^H + \mathbf{U}_n \mathbf{\Lambda}_n \mathbf{U}_n^H$$

其中：

- $M$ 个最大特征值 $\lambda_1 \geq \cdots \geq \lambda_M > \sigma^2$ 对应**信号子空间** $\mathbf{U}_s = [\mathbf{u}_1, \ldots, \mathbf{u}_M]$
- $N-M$ 个最小特征值 $\lambda_{M+1} = \cdots = \lambda_N = \sigma^2$ 对应**噪声子空间** $\mathbf{U}_n = [\mathbf{u}_{M+1}, \ldots, \mathbf{u}_N]$

关键性质：信号导向矢量张成的空间 $\text{span}\{\mathbf{A}(\boldsymbol{\theta})\}$ 等于信号子空间 $\text{span}\{\mathbf{U}_s\}$，且与噪声子空间正交：

$$\mathbf{a}^H(\theta_i) \mathbf{U}_n = \mathbf{0}, \quad \forall i = 1, \ldots, M$$

#### 第四步：MUSIC空间谱

利用正交性构造伪谱函数：

$$P_{\text{MUSIC}}(\theta, \phi) = \frac{1}{\mathbf{a}^H(\theta, \phi)\,\mathbf{U}_n\mathbf{U}_n^H\,\mathbf{a}(\theta, \phi)}$$

对 $(\theta, \phi)$ 进行二维谱峰搜索，$M$ 个峰值位置即为 $M$ 个信号源的来波方向估计。

---

### 算法三：Beamforming（波束赋形）算法

**目的**：通过调整阵列权重向量，增强目标方向信号、抑制其他方向的干扰与噪声，实现信号方向性增强与到达角估计。

**原理与公式推导**：

#### 第一步：阵列基础模型

$$\mathbf{x}(t) = \mathbf{a}(\theta)s(t) + \mathbf{n}(t)$$

#### 第二步：阵列输出信号

给定权重向量 $\mathbf{w} \in \mathbb{C}^{N\times1}$，阵列输出为：

$$y(t) = \mathbf{w}^H \mathbf{x}(t)$$

输出功率：

$$P = \mathbb{E}[|y(t)|^2] = \mathbf{w}^H \mathbf{R} \mathbf{w}$$

#### 第三步：两种权重优化策略

##### (1) CBF（常规波束形成，Conventional Beamforming）

直接令权重对准目标方向，使各路信号同相叠加：

$$\mathbf{w}_{\text{CBF}} = \frac{\mathbf{a}(\theta_0)}{N}$$

此时功率谱（扫描不同 $\theta$ 即可得DOA估计）：

$$P_{\text{CBF}}(\theta) = \frac{\mathbf{a}^H(\theta)\,\mathbf{R}\,\mathbf{a}(\theta)}{N^2}$$

**特点**：实现简单，但主瓣宽、旁瓣高，无法区分同一波束内的多信源，分辨率受限于波束宽度。

##### (2) MVDR（最小方差无失真响应，Minimum Variance Distortionless Response）

约束目标方向信号无失真通过（$|\mathbf{w}^H\mathbf{a}(\theta_0)| = 1$），同时最小化输出总功率（含干扰和噪声）：

$$\min_{\mathbf{w}} \; \mathbf{w}^H\mathbf{R}\mathbf{w} \quad \text{s.t.} \quad \mathbf{w}^H\mathbf{a}(\theta_0) = 1$$

利用拉格朗日乘子法求解，构造：

$$\mathcal{L}(\mathbf{w}, \lambda) = \mathbf{w}^H\mathbf{R}\mathbf{w} + \lambda(1 - \mathbf{w}^H\mathbf{a}(\theta_0))$$

对 $\mathbf{w}$ 求梯度并令其为零：

$$\frac{\partial\mathcal{L}}{\partial\mathbf{w}} = 2\mathbf{R}\mathbf{w} - \lambda\mathbf{a}(\theta_0) = 0 \;\Longrightarrow\; \mathbf{w} = \frac{\lambda}{2}\mathbf{R}^{-1}\mathbf{a}(\theta_0)$$

代入约束条件 $\mathbf{w}^H\mathbf{a}(\theta_0) = 1$ 确定 $\lambda$，最终得：

$$\boxed{\mathbf{w}_{\text{MVDR}} = \frac{\mathbf{R}^{-1}\mathbf{a}(\theta_0)}{\mathbf{a}^H(\theta_0)\mathbf{R}^{-1}\mathbf{a}(\theta_0)}}$$

对应MVDR功率谱：

$$P_{\text{MVDR}}(\theta) = \frac{1}{\mathbf{a}^H(\theta)\,\mathbf{R}^{-1}\,\mathbf{a}(\theta)}$$

**对比优势**：MVDR主瓣更窄（角度分辨率更高）、旁瓣被大幅抑制、能在干扰方向自适应形成零陷，在复杂电磁环境下实用性远优于CBF。


